node_modules
.env
.env.local
.env.production
dist
build

# Stray nested workspace copies — never commit these
Desktop/
local-commerce-platform/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Play Store secrets — never commit service account keys
google-service-account.json
*.keystore
*.jks